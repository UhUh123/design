// Backend URL. В dev — localhost:8000, в prod пока тоже (когда упакуем —
// backend будет запускаться как embedded subprocess из Electron).
export const API_BASE = 'http://localhost:8000'
export const WS_BASE = 'ws://localhost:8000'
